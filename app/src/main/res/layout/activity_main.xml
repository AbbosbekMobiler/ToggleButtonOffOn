<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/relativeL"
    android:background="@drawable/lampoff"
    tools:context=".MainActivity">
    <ToggleButton
        android:id="@+id/toggleBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="20dp"
        android:textColor="#E91E63"
        android:textOff="@string/lightoff"
        android:textOn="@string/lighton"
        android:drawableStart="@drawable/selate_item"
        android:layout_alignParentBottom="true"
        android:layout_margin="20dp"
        android:background="@drawable/bacground_btn_shape"
        android:layout_alignParentEnd="true"
        android:drawableLeft="@drawable/selate_item"/>
</RelativeLayout>